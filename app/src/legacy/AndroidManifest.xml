<?xml version="1.0" encoding="utf-8"?>
<!--
  legacy flavor 专属清单：传统 Xposed API(≤93) 框架通过以下 meta-data 识别模块。
  仅合并进 legacy 产物；normal/compatible（libxposed API 102）不携带这些声明，
  改由 META-INF/xposed/module.prop + java_init.list 声明。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <meta-data
            android:name="xposedmodule"
            android:value="true" />
        <meta-data
            android:name="xposeddescription"
            android:value="@string/module_description" />
        <meta-data
            android:name="xposedminversion"
            android:value="82" />
    </application>

</manifest>
